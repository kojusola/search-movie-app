import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: "Dm Sans",
    body: "Dm sans",
  },
  colors: {
    header: "#292929",
  },
});

export default theme;
